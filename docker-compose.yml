services:
  app:
    build: .
    ports:
      - "127.0.0.1:8000:8000"  
    env_file:
      - .env
    depends_on:
      - db  
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

  db:  
    image: arangodb
    environment:
      ARANGO_ROOT_PASSWORD: openSesame